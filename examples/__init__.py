"""Example scripts for graphworks.

This package is **not** shipped with the library — it is excluded from both the sdist and wheel
builds.  It exists only for local development use via ``uv run demo`` (or similar entry points
registered in ``pyproject.toml``).
"""
