"""Graphworks test suite.

Run with::

    uv run pytest
    # or, without uv:
    python -m pytest

The package must be installed (editable or otherwise) before running::

    uv sync
    # or:
    pip install -e .[dev]
"""
